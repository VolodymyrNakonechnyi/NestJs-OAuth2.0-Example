export const AUTH_PATTERNS = {
    LOGIN: 'auth.login',
    REGISTER: 'auth.register',
    VALIDATE_TOKEN: 'auth.validateToken',
    REFRESH_ACCESS: "auth.refreshAccess",
    LOGOUT: "auth.logout"
} as const;